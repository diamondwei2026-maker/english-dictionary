# 测试用例 — Task 3.3: 前端数据层集成

> 生成日期：2026-07-08

---

## 测试范围说明

本 Task 将前端所有页面的 mock 数据调用替换为真实后端 API 调用，涉及以下 API 端点：

| 端点 | 用途 | 权限 |
|------|------|------|
| `GET /api/v1/words` | 单词搜索 / 列表 | 公开 |
| `GET /api/v1/words/:id` | 单词详情 | 公开 |
| `POST /api/v1/words` | 新增单词 | admin |
| `PUT /api/v1/words/:id` | 编辑单词 | admin |
| `DELETE /api/v1/words/:id` | 删除单词 | admin |
| `GET /api/v1/wordbanks` | 词库列表 | 公开 |
| `GET /api/v1/wordbanks/:id` | 词库详情 | 公开 |
| `GET /api/v1/wordbanks/:id/words` | 词库下单词 | 公开 |
| `POST /api/v1/wordbanks` | 新增词库 | admin |
| `PUT /api/v1/wordbanks/:id` | 编辑词库 | admin |
| `DELETE /api/v1/wordbanks/:id` | 删除词库 | admin |

---

## 一、用户端页面 — 首页

### TC-001: 首页"今日一词"从 API 获取
- **类型**: 功能测试
- **关联验收标准**: 首页「今日一词」从 API 获取
- **前置条件**: 数据库中至少有 1 个单词（通过 seed 脚本或手动添加）
- **输入**:
  - 访问首页（`/pages/home/index`）
- **执行步骤**:
  1. 打开应用，进入首页
  2. 观察"今日一词"卡片区域
- **预期输出**:
  - "今日一词"卡片展示一个单词的名称、音标、核心义描述
  - 数据来自 `GET /api/v1/words?pageSize=1`（或随机单词接口）
  - 展示字段：word、phonetic、coreMeaning
- **清理**: 无

### TC-002: 首页单词搜索调用真实 API
- **类型**: 功能测试
- **关联验收标准**: 单词搜索页调用 `GET /api/v1/words?q=keyword` 真实搜索
- **前置条件**: 数据库中存在单词 "flow"
- **输入**:
  - 在搜索框输入 "flow"
- **执行步骤**:
  1. 打开首页
  2. 在搜索框输入 "flow"
  3. 观察搜索结果列表
- **预期输出**:
  - 调用 `GET /api/v1/words?q=flow`
  - 搜索结果中包含单词 "flow"
  - 每条结果显示 word、phonetic、coreMeaning（截断）
  - 点击可跳转至单词详情页
- **清理**: 无

### TC-003: 搜索无匹配单词时显示空状态
- **类型**: 边界测试
- **关联验收标准**: 列表为空时显示空状态提示
- **前置条件**: 数据库正常运行
- **输入**:
  - 在搜索框输入 "zzzzzzzz_not_exist"
- **执行步骤**:
  1. 打开首页
  2. 输入不存在的关键词
  3. 观察结果区域
- **预期输出**:
  - 显示"未找到相关单词"提示
  - 显示"可在管理后台添加新词汇"辅助文案
- **清理**: 无

### TC-004: 首页"全部词汇"列表从 API 获取
- **类型**: 功能测试
- **关联验收标准**: 前端不再依赖 mock 数据文件
- **前置条件**: 数据库中有多个单词
- **输入**:
  - 访问首页，处于非搜索状态
- **执行步骤**:
  1. 打开首页
  2. 滚动至"全部词汇"区域
- **预期输出**:
  - 展示单词列表，数据来自 `GET /api/v1/words`
  - 每条显示 word、phonetic、coreMeaning
  - 显示关联的词库名称标签
  - 显示总单词数（来自 pagination.total）
- **清理**: 无

### TC-005: 首页数据加载中显示 loading 状态
- **类型**: 功能测试
- **关联验收标准**: 数据加载中显示 loading 状态
- **前置条件**: 可模拟慢速网络
- **输入**:
  - 访问首页
- **执行步骤**:
  1. 打开首页
  2. 在网络请求返回前观察页面
- **预期输出**:
  - 显示 loading 示意（骨架屏或加载指示器）
  - API 返回后 loading 消失，展示数据
- **清理**: 无

### TC-006: 首页 API 请求失败时显示错误与重试
- **类型**: 异常测试
- **关联验收标准**: 网络错误时显示错误提示 + 重试按钮
- **前置条件**: 模拟 API 请求失败（断网或后端不可达）
- **输入**:
  - 访问首页（后端不可达）
- **执行步骤**:
  1. 断开网络或停止后端服务
  2. 打开首页
  3. 观察错误态
  4. 点击重试按钮
- **预期输出**:
  - 显示错误提示信息
  - 提供重试按钮
  - 点击重试后重新发起 API 请求
- **清理**: 恢复网络/后端服务

---

## 二、用户端页面 — 单词详情

### TC-007: 单词详情页调用 API 获取完整数据
- **类型**: 功能测试
- **关联验收标准**: 单词详情页调用 `GET /api/v1/words/:id` 获取完整数据
- **前置条件**: 数据库中存在单词（id 已知，如通过 seed 创建的 "flow"）
- **输入**:
  - 导航至 `/pages/word-detail/index?wordId=<word_id>`
- **执行步骤**:
  1. 从首页搜索或词库列表点击单词进入详情页
  2. 观察展示内容
- **预期输出**:
  - 调用 `GET /api/v1/words/:id`
  - 展示：word（单词名）、phonetic（音标）、coreMeaning（核心义）
  - 展示 coreImageType（物理意象图）
  - 展示 coreExampleSentence / coreExampleTranslation（核心义例句）
  - 展示 extendedMeanings 数组（引申义列表，含 logicalEvolution、meaning、partOfSpeech、exampleSentence、exampleTranslation）
  - 展示 collocations（常见搭配）
  - 展示关联词库名称
- **清理**: 无

### TC-008: 单词不存在时友好提示
- **类型**: 异常测试
- **关联验收标准**: 404 单词/词库不存在时友好提示
- **前置条件**: 无
- **输入**:
  - 导航至 `/pages/word-detail/index?wordId=nonexistent-id`
- **执行步骤**:
  1. 通过 URL 直接访问不存在的单词详情
  2. 观察页面
- **预期输出**:
  - 显示"单词不存在"或类似的友好提示
  - 提供返回首页的入口
- **清理**: 无

### TC-009: 单词详情加载中状态
- **类型**: 功能测试
- **关联验收标准**: 数据加载中显示 loading 状态
- **前置条件**: 可模拟慢速网络
- **输入**:
  - 点击任意单词进入详情
- **执行步骤**:
  1. 点击单词进入详情页
  2. 网络请求返回前观察
- **预期输出**:
  - 显示 loading 状态
  - API 返回后展示完整详情
- **清理**: 无

---

## 三、用户端页面 — 词库列表

### TC-010: 词库列表页调用 API 展示词库
- **类型**: 功能测试
- **关联验收标准**: 词库列表页调用 `GET /api/v1/wordbanks` 展示公开词库
- **前置条件**: 数据库中有多个词库（通过 seed 脚本）
- **输入**:
  - 导航至词库页面（`/pages/libraries/index`）
- **执行步骤**:
  1. 点击底部 Tab 进入词库列表
  2. 观察展示内容
- **预期输出**:
  - 调用 `GET /api/v1/wordbanks`
  - 每个词库卡片显示：name、description、wordCount
  - 点击词库卡片跳转至词库下单词列表
  - 非 admin 用户只能看到公开词库
- **清理**: 无

### TC-011: 词库列表为空时显示空状态
- **类型**: 边界测试
- **关联验收标准**: 列表为空时显示空状态提示
- **前置条件**: 清空数据库中所有公开词库
- **输入**:
  - 导航至词库页面
- **执行步骤**:
  1. 打开词库页面
  2. 观察页面
- **预期输出**:
  - 显示空状态提示（如"暂无词库"或类似文案）
- **清理**: 恢复数据

### TC-012: 词库列表加载中状态
- **类型**: 功能测试
- **关联验收标准**: 数据加载中显示 loading 状态
- **前置条件**: 可模拟慢速网络
- **输入**:
  - 导航至词库页面
- **执行步骤**:
  1. 进入词库页面
  2. 在 API 返回前观察
- **预期输出**:
  - 显示 loading 状态
  - API 返回后展示词库列表
- **清理**: 无

### TC-013: 词库列表 API 请求失败时显示错误与重试
- **类型**: 异常测试
- **关联验收标准**: 网络错误时显示错误提示 + 重试按钮
- **前置条件**: 模拟后端不可达
- **输入**:
  - 导航至词库页面
- **执行步骤**:
  1. 断开网络
  2. 进入词库页面
  3. 观察错误态
  4. 点击重试
- **预期输出**:
  - 显示错误提示
  - 提供重试按钮
  - 点击重试重新请求
- **清理**: 恢复网络

---

## 四、用户端页面 — 词库下单词列表

### TC-014: 词库下单词列表页调用 API
- **类型**: 功能测试
- **关联验收标准**: 词库下单词列表页调用 `GET /api/v1/wordbanks/:id/words`
- **前置条件**: 数据库中有词库且包含单词
- **输入**:
  - 导航至 `/pages/library-words/index?libraryId=<wordbank_id>`
- **执行步骤**:
  1. 从词库列表点击某个词库进入
  2. 观察页面
- **预期输出**:
  - 调用 `GET /api/v1/wordbanks/:id/words`
  - 展示该词库下的单词列表
  - 每条显示 word、phonetic、coreMeaning（截断）
  - 点击跳转至单词详情
  - 显示单词总数
- **清理**: 无

### TC-015: 词库下无单词时显示空状态
- **类型**: 边界测试
- **关联验收标准**: 列表为空时显示空状态提示
- **前置条件**: 存在一个空词库（新建且未添加单词）
- **输入**:
  - 导航至空词库的单词列表页
- **执行步骤**:
  1. 进入空词库
  2. 观察页面
- **预期输出**:
  - 显示"该词库暂无单词"空状态
- **清理**: 无

### TC-016: 词库不存在时友好提示
- **类型**: 异常测试
- **关联验收标准**: 404 单词/词库不存在时友好提示
- **前置条件**: 无
- **输入**:
  - 导航至 `/pages/library-words/index?libraryId=nonexistent-id`
- **执行步骤**:
  1. 通过 URL 直接访问不存在的词库
  2. 观察页面
- **预期输出**:
  - 显示"词库不存在"或类似提示
  - 提供返回词库列表的入口
- **清理**: 无

---

## 五、管理后台 — 词库管理

### TC-017: 管理后台词库列表从 API 获取
- **类型**: 功能测试
- **关联验收标准**: 管理后台：词库管理页 CRUD 操作调用真实 API
- **前置条件**: admin 用户已登录；数据库中有词库
- **输入**:
  - 导航至管理后台 → 词库管理
- **执行步骤**:
  1. admin 用户登录
  2. 进入管理后台
  3. 点击"词库管理"
  4. 观察词库列表
- **预期输出**:
  - 调用 `GET /api/v1/wordbanks`（admin 可看到公开+私有词库）
  - 展示所有词库（含公开和私有）
  - 每条显示 name、description、该词库下单词数
- **清理**: 无

### TC-018: 新增词库调用 POST API
- **类型**: 功能测试
- **关联验收标准**: 词库列表、新增、编辑、删除可用
- **前置条件**: admin 已登录
- **输入**:
  - 词库名称："测试新增词库"
  - 词库描述："这是一个测试用词库"
- **执行步骤**:
  1. 进入词库管理页
  2. 点击"+ 新增"按钮
  3. 填写词库名称和描述
  4. 点击"保存"
- **预期输出**:
  - 调用 `POST /api/v1/wordbanks`，请求体含 name、description
  - 返回 201，新词库出现在列表中
  - 显示成功提示
- **清理**: 删除新增的测试词库

### TC-019: 编辑词库调用 PUT API
- **类型**: 功能测试
- **关联验收标准**: 词库列表、新增、编辑、删除可用
- **前置条件**: admin 已登录；存在可编辑的词库
- **输入**:
  - 修改词库名称为"已更新的词库名"
- **执行步骤**:
  1. 进入词库管理页
  2. 点击某词库的"编辑"按钮
  3. 修改名称
  4. 点击"保存"
- **预期输出**:
  - 调用 `PUT /api/v1/wordbanks/:id`，请求体含更新字段
  - 返回更新后的词库对象
  - 列表中该词库名称已更新
  - 显示成功提示
- **清理**: 恢复原始名称

### TC-020: 删除词库调用 DELETE API
- **类型**: 功能测试
- **关联验收标准**: 词库列表、新增、编辑、删除可用
- **前置条件**: admin 已登录；存在可删除的词库
- **输入**:
  - 选择要删除的词库
- **执行步骤**:
  1. 进入词库管理页
  2. 点击某词库的"删除"按钮
  3. 在确认弹窗中确认
- **预期输出**:
  - 弹出确认对话框
  - 确认后调用 `DELETE /api/v1/wordbanks/:id`
  - 返回 `{ message: "词库已删除" }`
  - 该词库从列表中移除
  - 显示操作成功反馈
- **清理**: 无（已删除）

### TC-021: 新增词库时名称为空校验
- **类型**: 边界测试
- **关联验收标准**: 操作反馈（成功提示/失败提示）正常
- **前置条件**: admin 已登录
- **输入**:
  - 词库名称留空
- **执行步骤**:
  1. 进入新增词库页面
  2. 不填写名称，直接点击保存
- **预期输出**:
  - 保存按钮不可点击（disabled）或点击后提示"名称不能为空"
- **清理**: 无

### TC-022: 非 admin 用户无法管理词库
- **类型**: 异常测试
- **关联验收标准**: 管理后台：词库管理页 CRUD 操作调用真实 API（权限校验）
- **前置条件**: 使用普通 user 账号登录
- **输入**:
  - 普通用户尝试新增/编辑/删除词库
- **执行步骤**:
  1. 普通用户登录
  2. 尝试直接调用管理接口
- **预期输出**:
  - 前端层面：普通用户看不到管理后台入口
  - API 层面：直接调用返回 403（adminMiddleware 拦截）
- **清理**: 无

---

## 六、管理后台 — 单词管理

### TC-023: 管理后台单词列表从 API 获取
- **类型**: 功能测试
- **关联验收标准**: 管理后台：单词管理页 CRUD 操作调用真实 API
- **前置条件**: admin 已登录；数据库中有单词
- **输入**:
  - 导航至管理后台 → 单词管理
- **执行步骤**:
  1. admin 进入管理后台
  2. 点击"单词管理"
  3. 观察单词列表
- **预期输出**:
  - 调用 `GET /api/v1/words`（admin 可看到所有词库的单词）
  - 展示单词列表，含 word、phonetic、coreMeaning、所属词库、引申义数量
- **清理**: 无

### TC-024: 管理后台按词库筛选单词
- **类型**: 功能测试
- **关联验收标准**: 单词列表（支持按词库筛选）
- **前置条件**: admin 已登录；存在多个词库各有单词
- **输入**:
  - 在单词管理页选择某词库筛选
- **执行步骤**:
  1. 进入单词管理页
  2. 使用筛选功能选择特定词库
  3. 观察列表
- **预期输出**:
  - 调用 `GET /api/v1/words?wordbank_id=<id>`
  - 仅展示该词库下的单词
- **清理**: 无

### TC-025: 新增单词调用 POST API
- **类型**: 功能测试
- **关联验收标准**: 单词列表、新增、编辑、删除可用
- **前置条件**: admin 已登录；存在词库
- **输入**:
  - 单词："test"
  - 选择所属词库
  - 填写核心义、音标等
- **执行步骤**:
  1. 进入单词管理页
  2. 点击"+ 新增"按钮
  3. 填写单词信息（word、phonetic、coreMeaning、libraryId 等）
  4. 点击"保存单词"
- **预期输出**:
  - 调用 `POST /api/v1/words`，请求体含完整单词数据
  - 返回 201，新单词出现在列表中
  - 显示成功提示
- **清理**: 删除新增的测试单词

### TC-026: 编辑单词调用 PUT API
- **类型**: 功能测试
- **关联验收标准**: 单词列表、新增、编辑、删除可用
- **前置条件**: admin 已登录；存在可编辑的单词
- **输入**:
  - 修改单词的 coreMeaning
- **执行步骤**:
  1. 进入单词管理页
  2. 点击某单词的"编辑"
  3. 修改核心义描述
  4. 点击"保存单词"
- **预期输出**:
  - 调用 `PUT /api/v1/words/:id`，请求体含更新字段
  - 返回更新后的单词对象
  - 列表中该单词数据已更新
  - 显示成功提示
- **清理**: 恢复原始数据

### TC-027: 删除单词调用 DELETE API
- **类型**: 功能测试
- **关联验收标准**: 单词列表、新增、编辑、删除可用
- **前置条件**: admin 已登录；存在可删除的单词
- **输入**:
  - 选择要删除的单词
- **执行步骤**:
  1. 进入单词管理页
  2. 点击某单词的"删除"
  3. 确认删除
- **预期输出**:
  - 弹出确认对话框
  - 确认后调用 `DELETE /api/v1/words/:id`
  - 返回 `{ message: "单词已删除" }`
  - 该单词从列表中移除
  - 显示操作成功反馈
- **清理**: 无（已删除）

### TC-028: 新增单词时必填字段校验
- **类型**: 边界测试
- **关联验收标准**: 操作反馈（成功提示/失败提示）正常
- **前置条件**: admin 已登录
- **输入**:
  - word 为空或不填
- **执行步骤**:
  1. 进入新增单词页面
  2. 不填写单词名，点击保存
- **预期输出**:
  - 保存按钮 disabled 或提示必填字段缺失
- **清理**: 无

### TC-029: AI 生成词条功能（管理后台）
- **类型**: 功能测试
- **关联验收标准**: 管理后台：单词管理页 CRUD 操作调用真实 API（AI 生成部分后续 Task 4.1 完整实现，此处仅验证 UI 残留不影响 API 集成）
- **前置条件**: admin 已登录
- **输入**:
  - 在新增单词页面输入单词名
- **执行步骤**:
  1. 进入新增单词页面
  2. 输入英文单词
  3. 点击"AI 自动生成词条"
- **预期输出**:
  - Task 3.3 阶段保留前端 mock AI 逻辑（不调用后端 AI API）
  - 生成后表单字段自动填充
  - 用户可继续编辑后保存
- **清理**: 无

---

## 七、管理后台 — 数据概览

### TC-030: 管理后台概览数据从 API 获取
- **类型**: 功能测试
- **关联验收标准**: 前端不再依赖 mock 数据文件
- **前置条件**: admin 已登录
- **输入**:
  - 导航至管理后台概览页
- **执行步骤**:
  1. admin 进入管理后台
  2. 观察概览页统计数据
- **预期输出**:
  - 词库数、单词数来自 API（`GET /api/v1/wordbanks` + `GET /api/v1/words` 的 pagination.total）
  - 用户数来自 admin-only 用户列表 API（如 `GET /api/v1/users`）
  - 点击"词库管理"/"单词管理"可进入对应管理页
- **清理**: 无

---

## 八、状态管理 — Loading / Error / Empty

### TC-031: API 请求 loading 状态全局覆盖
- **类型**: 功能测试
- **关联验收标准**: API 调用增加 loading/error 状态处理
- **前置条件**: 所有页面完成 API 集成
- **输入**:
  - 依次访问首页、词库列表、单词详情、管理后台各页面
- **执行步骤**:
  1. 在慢速网络下依次访问各页面
  2. 观察加载态
- **预期输出**:
  - 每个页面在数据加载期间显示 loading 状态
  - 不得出现空白页面或"undefined"等异常文案
- **清理**: 无

### TC-032: API 请求 error 状态全局覆盖
- **类型**: 异常测试
- **关联验收标准**: 网络错误时显示错误提示 + 重试按钮
- **前置条件**: 模拟后端不可达
- **输入**:
  - 依次访问各页面
- **执行步骤**:
  1. 停止后端服务
  2. 依次访问各页面
  3. 观察错误态
- **预期输出**:
  - 每个页面显示错误提示（而非白屏或 crash）
  - 提供重试按钮
- **清理**: 启动后端服务

### TC-033: 空数据状态正常展示
- **类型**: 边界测试
- **关联验收标准**: 列表为空时显示空状态提示
- **前置条件**: 清空数据库
- **输入**:
  - 依次访问各列表页面
- **执行步骤**:
  1. 清空数据库
  2. 访问首页、词库列表、词库下单词、管理后台列表
- **预期输出**:
  - 各页面显示对应的空状态提示
  - 不出现空白或报错
- **清理**: 重新运行 seed 脚本

---

## 九、认证与权限集成

### TC-034: 管理后台入口仅 admin 可见
- **类型**: 功能测试
- **关联验收标准**: 管理后台功能集成
- **前置条件**: 存在普通 user 和 admin 两种账号
- **输入**:
  - 分别以 user 和 admin 登录
- **执行步骤**:
  1. 普通用户登录，查看底部 Tab 或个人中心
  2. admin 用户登录，查看底部 Tab 或个人中心
- **预期输出**:
  - 普通用户：不显示管理后台入口
  - admin 用户：显示管理后台入口
- **清理**: 无

### TC-035: 管理 API 鉴权失败全局处理
- **类型**: 异常测试
- **关联验收标准**: 管理后台权限校验
- **前置条件**: request.ts 已集成 401 全局处理
- **输入**:
  - admin token 过期后尝试操作
- **执行步骤**:
  1. admin 登录后等待 token 过期（或手动清除 token）
  2. 尝试新增词库/单词
- **预期输出**:
  - 收到 401 响应
  - 自动清除 token 并跳转登录页
  - 显示"认证失败，请重新登录"提示
- **清理**: 无

---

## 十、向后兼容

### TC-036: mock 数据文件保留但不再被导入
- **类型**: 功能测试
- **关联验收标准**: 前端不再依赖 mock 数据文件（但保留以备测试参考）
- **前置条件**: 所有页面完成 API 集成
- **输入**:
  - 检查各页面 import 语句
- **执行步骤**:
  1. 搜索 `client/src/pages/` 下所有 `.tsx` 文件
  2. 确认 import 来源
- **预期输出**:
  - 页面组件不再 `import { mockWords, mockLibraries } from "../../data/mockData"`
  - `mockData.ts` 文件仍保留
  - 若引用 `types.ts` 中的类型定义可保留
- **清理**: 无

---

## 测试用例统计

| 类型 | 数量 |
|------|------|
| 功能测试 | 20 |
| 边界测试 | 7 |
| 异常测试 | 6 |
| 集成测试 | 3 |
| **合计** | **36** |

### 按页面/模块分布

| 模块 | 用例数 |
|------|--------|
| 首页 | 6 (TC-001 ~ TC-006) |
| 单词详情 | 3 (TC-007 ~ TC-009) |
| 词库列表 | 4 (TC-010 ~ TC-013) |
| 词库下单词 | 3 (TC-014 ~ TC-016) |
| 管理后台-词库 | 6 (TC-017 ~ TC-022) |
| 管理后台-单词 | 7 (TC-023 ~ TC-029) |
| 管理后台-概览 | 1 (TC-030) |
| Loading/Error/Empty 全局 | 3 (TC-031 ~ TC-033) |
| 认证与权限 | 2 (TC-034 ~ TC-035) |
| 向后兼容 | 1 (TC-036) |
