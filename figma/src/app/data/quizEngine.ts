import { mockQuizItems } from './mockData';
import type { QuizDirection, QuizItem, QuizResult } from './types';

const normalize = (value: string) => value.toLowerCase().trim().replace(/[.!?。！？]+$/g, '').replace(/\s+/g, ' ');
const words = (value: string) => normalize(value).match(/[a-z]+(?:'[a-z]+)?/g) ?? [];
const variantMatches = (keyword: string, input: string[]) => input.some(word => word === keyword || word.replace(/(s|es|ed|ing)$/,'') === keyword.replace(/(s|es|ed|ing)$/,''));
const lcs = (a: string[], b: string[]) => { const table = Array.from({ length: a.length + 1 }, () => Array(b.length + 1).fill(0)); for (let i=1;i<=a.length;i++) for (let j=1;j<=b.length;j++) table[i][j] = a[i-1]===b[j-1] ? table[i-1][j-1]+1 : Math.max(table[i-1][j],table[i][j-1]); return table[a.length][b.length]; };
const shuffle = <T,>(values:T[]) => [...values].sort(() => Math.random() - .5);
export function generateQuiz(direction: QuizDirection, wordId?: string): QuizItem[] { const all = mockQuizItems.filter(item => item.direction === direction); const focused = wordId ? all.filter(item => item.wordId === wordId) : []; const pool = focused.length ? focused : all; const shuffled = shuffle(pool); return Array.from({ length: 10 }, (_, index) => ({ ...shuffled[index % shuffled.length], id: `${shuffled[index % shuffled.length].id}-${index}` })); }
export function judgeAnswer(item: QuizItem, userInput: string, direction: QuizDirection): QuizResult { if (!normalize(userInput)) return { correct:false, score:0, matched:[], missing:item.keywords, analysis:`${item.analysis} 你还没有输入答案；参考表达：${item.reference}` }; const inputWords=words(userInput); if (direction === 'en2zh') { const source=normalize(userInput), ref=normalize(item.reference), score=Math.round([...new Set(source)].filter(c=>ref.includes(c)).length/Math.max(ref.length,1)*100); return {correct:score>=70,score,matched:[],missing:[],analysis:score>=70?`表达正确。参考答案：${item.reference}`:`${item.analysis} 参考答案：${item.reference}`}; } const matched=item.keywords.filter(k=>variantMatches(k,inputWords)); const missing=item.keywords.filter(k=>!matched.includes(k)); const keywordScore=(matched.length/item.keywords.length)*70; const referenceWords=words(item.reference); const sequenceScore=(lcs(inputWords, referenceWords)/Math.max(referenceWords.length,1))*30; const score=Math.round(keywordScore+sequenceScore); const correct=score>=70; return {correct,score,matched,missing,analysis:correct?`表达已经抓住了这句的核心意象。参考答案：${item.reference}`:`${item.analysis}${missing.length ? ` 建议补上：${missing.join('、')}。` : ''} 参考答案：${item.reference}`}; }
