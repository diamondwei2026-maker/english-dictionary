/* === Pre-generated CSS for pages/word-detail/word-detail.vue === */
/* Source: pages/word-detail/index.tsx */
/* Agent: PASTE this entire block into <style scoped> of pages/word-detail/word-detail.vue */

/* Source line 30 — View */
.word-detail__el1 {
  display: flex;
  align-items: center;
  gap: 12rpx;
  margin: 24rpx20rpx;
}

/* Source line 31 — View */
.word-detail__el2 {
  height: 2rpx;
  flex: 1;
  background: linear-gradient(toright,#E5E7EB,#2563EB);
}

/* Source line 124 — View */
.word-detail__el3 {
  min-height: 100vh;
  background: #F7F9FC;
}

/* Source line 127 — unknown */
.word-detail__el4 {
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  min-height: 70vh;
  color: #9CA3AF;
}

/* Source line 137 — Text */
.word-detail__el5 {
  font-size: 30rpx;
  margin-top: 24rpx;
  display: block;
}

/* Source line 149 — View */
.word-detail__el6 {
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  height: 100vh;
  color: #6B7280;
}

/* Source line 150 — Text */
.word-detail__el7 {
  display: block;
}

/* Source line 153 — unknown */
.word-detail__el8 {
  margin-top: 24rpx;
  color: #2563EB;
}

/* Source line 164 — View */
.word-detail__el9 {
  min-height: 100vh;
  background: #F7F9FC;
}

/* Source line 167 — unknown */
.word-detail__el10 {
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  min-height: 70vh;
  padding: 48rpx;
}

/* Source line 177 — unknown */
.word-detail__el11 {
  font-size: 30rpx;
  color: #DC2626;
  text-align: center;
  display: block;
  margin-bottom: 40rpx;
}

/* Source line 189 — unknown */
.word-detail__el12 {
  padding: 24rpx64rpx;
  background: #2563EB;
  color: #fff;
  border-radius: 40rpx;
  font-size: 28rpx;
  font-weight: 600;
}

/* Source line 198 — Text */
.word-detail__el13 {
  color: #fff;
}

/* Source line 207 — View */
.word-detail__el14 {
  min-height: 100vh;
  background: #F7F9FC;
}

/* Source line 216 — Text */
.word-detail__el15 {
  font-size: 22rpx;
  color: #2563EB;
  background: #EFF6FF;
  padding: 8rpx24rpx;
  border-radius: 40rpx;
  letter-spacing: 0.6rpx;
}

/* Source line 229 — View */
.word-detail__el16 {
  padding: 16rpx48rpx80rpx;
}

/* Source line 231 — View */
.word-detail__el17 {
  margin-bottom: 48rpx;
}

/* Source line 232 — View */
.word-detail__el18 {
  display: flex;
  align-items: baseline;
  justify-content: space-between;
  margin-bottom: 8rpx;
}

/* Source line 233 — Text */
.word-detail__el19 {
  font-size: 84rpx;
  font-weight: 800;
  color: #111827;
  display: block;
  letter-spacing: -2rpx;
  line-height: 1.1;
}

/* Source line 236 — View */
.word-detail__el20 {
  display: flex;
  align-items: center;
  justify-content: center;
  width: 80rpx;
  height: 80rpx;
  border-radius: 50%;
  background: isFavorited?"#FEF3C7":"#F9FAFB";
  flex-shrink: 0;
  margin-left: 24rpx;
  opacity: favLoading?0.6:1;
}

/* Source line 251 — Text */
.word-detail__el21 {
  font-size: 32rpx;
  color: #9CA3AF;
  display: block;
  letter-spacing: 1rpx;
}

/* Source line 257 — View */
.word-detail__el22 {
  margin-bottom: 48rpx;
}

/* Source line 258 — Text */
.word-detail__el23 {
  font-size: 22rpx;
  font-weight: 600;
  color: #9CA3AF;
  letter-spacing: 4rpx;
  text-transform: uppercase;
  display: block;
  margin-bottom: 24rpx;
}

/* Source line 261 — View */
.word-detail__el24 {
  border-radius: 48rpx;
  overflow: hidden;
  box-shadow: 8rpx48rpxrgba(0, 0, 0, 0.07);
}

/* Source line 271 — View */
.word-detail__el25 {
  background: #fff;
  border-radius: 48rpx;
  padding: 48rpx;
  margin-bottom: 32rpx;
  box-shadow: 4rpx40rpxrgba(0, 0, 0, 0.05);
}

/* Source line 278 — Text */
.word-detail__el26 {
  font-size: 22rpx;
  font-weight: 600;
  color: #9CA3AF;
  letter-spacing: 4rpx;
  text-transform: uppercase;
  display: block;
  margin-bottom: 24rpx;
}

/* Source line 281 — Text */
.word-detail__el27 {
  font-size: 32rpx;
  color: #111827;
  line-height: 1.7;
  display: block;
  margin-bottom: 40rpx;
  font-weight: 500;
}

/* Source line 284 — View */
.word-detail__el28 {
  padding: 32rpx36rpx;
  background: #F8FAFC;
  border-radius: 28rpx;
  border-left: 6rpx solid#2563EB;
}

/* Source line 290 — Text */
.word-detail__el29 {
  font-size: 30rpx;
  color: #1D4ED8;
  display: block;
  margin-bottom: 12rpx;
  font-style: italic;
  line-height: 1.6;
}

/* Source line 293 — Text */
.word-detail__el30 {
  font-size: 26rpx;
  color: #6B7280;
  display: block;
  line-height: 1.6;
}

/* Source line 300 — View */
.word-detail__el31 {
  margin-bottom: 32rpx;
}

/* Source line 301 — Text */
.word-detail__el32 {
  font-size: 22rpx;
  font-weight: 600;
  color: #9CA3AF;
  letter-spacing: 4rpx;
  text-transform: uppercase;
  display: block;
  margin-bottom: 24rpx;
}

/* Source line 304 — View */
.word-detail__el33 {
  display: flex;
  flex-direction: column;
  gap: 24rpx;
}

/* Source line 308 — unknown */
.word-detail__el34 {
  background: #fff;
  border-radius: 40rpx;
  padding: 40rpx;
  box-shadow: 4rpx32rpxrgba(0, 0, 0, 0.04);
}

/* Source line 316 — View */
.word-detail__el35 {
  padding: 20rpx28rpx;
  background: #F8FAFC;
  border-radius: 20rpx;
  margin-bottom: 28rpx;
}

/* Source line 322 — Text */
.word-detail__el36 {
  font-size: 24rpx;
  color: #6B7280;
  display: block;
  line-height: 1.6;
}

/* Source line 323 — Text */
.word-detail__el37 {
  color: #9CA3AF;
  margin-right: 12rpx;
}

/* Source line 331 — View */
.word-detail__el38 {
  display: flex;
  align-items: flex-start;
  justify-content: space-between;
  gap: 24rpx;
  margin-bottom: 28rpx;
}

/* Source line 332 — Text */
.word-detail__el39 {
  font-size: 32rpx;
  font-weight: 600;
  color: #111827;
  display: block;
  line-height: 1.5;
  flex: 1;
}

/* Source line 335 — Text */
.word-detail__el40 {
  flex-shrink: 0;
  font-size: 22rpx;
  padding: 6rpx20rpx;
  border-radius: 40rpx;
  font-weight: 500;
  background: (POS_COLORS[ext.partOfSpeech]|| bg: "#F3F4F6", text: "#6B7280" ).bg;
  color: (POS_COLORS[ext.partOfSpeech]|| bg: "#F3F4F6", text: "#6B7280" ).text;
}

/* Source line 349 — View */
.word-detail__el41 {
  padding: 28rpx32rpx;
  background: #F8FAFC;
  border-radius: 24rpx;
  border-left: 6rpx solid#10B981;
}

/* Source line 355 — Text */
.word-detail__el42 {
  font-size: 28rpx;
  color: #065F46;
  display: block;
  margin-bottom: 12rpx;
  font-style: italic;
  line-height: 1.6;
}

/* Source line 358 — Text */
.word-detail__el43 {
  font-size: 26rpx;
  color: #6B7280;
  display: block;
  line-height: 1.6;
}

/* Source line 368 — View */
.word-detail__el44 {
  background: #fff;
  border-radius: 48rpx;
  padding: 48rpx;
  box-shadow: 4rpx40rpxrgba(0, 0, 0, 0.05);
}

/* Source line 374 — Text */
.word-detail__el45 {
  font-size: 22rpx;
  font-weight: 600;
  color: #9CA3AF;
  letter-spacing: 4rpx;
  text-transform: uppercase;
  display: block;
  margin-bottom: 32rpx;
}

/* Source line 377 — View */
.word-detail__el46 {
  display: flex;
  flex-wrap: wrap;
  gap: 16rpx;
}

/* Source line 381 — unknown */
.word-detail__el47 {
  padding: 16rpx28rpx;
  background: #F1F5F9;
  border-radius: 24rpx;
  font-size: 28rpx;
  color: #374151;
  font-style: italic;
  line-height: 1;
}

